ls
cp toto_to_born toto 
echo "Fred toto">toto
cat toto
rm toto

cat /etc/passwd|grep root
ls|grep -o run.sh|fgrep -o run.s|egrep -o run
ls -al -h|rev
ls|rev
ls -alh|cat|rev|cat|rev
cp -v toto_to_born2  toto|rev 
echo bonjour | cat\n
env |grep 'PWD'

 atest='1 2 3'
 env |grep 'atest'
  env|grep atest

ls|grep root>>t.txt
cat t.txt
ls -al -h
rev </etc/passwd
  atest=toto
   da"ns $PWD, voilà" !

echo     da"ns $PWD, voilà" !
echo     "   $PWD, voilà" !
echo     '   $PWD, voilà' !
echo "fred

echo "mic'mac" dis donc' là
  a=1
  b=2
  c=3
  echo "$a$b$c"
  echo "$a'$b" dis $c' là
echo bonjour | cat
env |grep 'PWD'

  a=1
  b=2
  c=3
  d=/
  cd $d
  pwd
  echo "$PWD $a$b$c"
